.table-content
  = render 'areas_map'
= render 'shared/notification'
-#%div
  %label.title{ :for => 'filterIdeas'} Filtruj pomysły wg
  = select "name", "idea_status_id", idea_statuses(), { selected: idea_sort_and_filter_params }
.table-content
  .uk-overflow-container.table-inside
    %table.uk-table#dataTable{data: {"page-length" => 25}}
      %thead
        %tr
          %th
            %h4 Pole
          %th
            %h4 Powierzchnia
          %th
            %h4 Klasa
          %th
            %h4 Status
          %th{data: {orderable: "false", searchable: "false"}}
            %h4 akcje
      %tbody
        = render @areas
    = link_to 'Dodaj nowe pole', new_area_path, class: 'uk-button uk-button-primary uk-button-large'
#area_modal.uk-modal
= render 'testt'
